/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type { RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import fetch from "@kubb/plugin-client/clients/axios";
import type {
  GetItem400,
  GetItem422,
  GetItemQueryParams,
  GetItemQueryResponse,
} from "@/gen/types/get-item";

function getGetItemUrl() {
  const res = { method: "GET", url: `/store/items` as const };
  return res;
}

/**
 * @summary Retrieve a single item.
 * {@link /store/items}
 */
export async function getItem(
  params: GetItemQueryParams,
  config: Partial<RequestConfig> & { client?: typeof fetch } = {}
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetItemQueryResponse,
    ResponseErrorConfig<GetItem400 | GetItem422>,
    unknown
  >({ method: "GET", url: getGetItemUrl().url.toString(), params, ...requestConfig });
  return res.data;
}
