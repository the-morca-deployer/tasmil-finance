/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type fetch from "@kubb/plugin-client/clients/axios";
import type { RequestConfig, ResponseErrorConfig } from "@kubb/plugin-client/clients/axios";
import type {
  QueryClient,
  QueryKey,
  QueryObserverOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import { queryOptions, useQuery } from "@tanstack/react-query";
import { getItem } from "@/gen/client/get-item";
import type {
  GetItem400,
  GetItem422,
  GetItemQueryParams,
  GetItemQueryResponse,
} from "@/gen/types/get-item";

export const getItemQueryKey = (params: GetItemQueryParams) =>
  [{ url: "/store/items" }, ...(params ? [params] : [])] as const;

export type GetItemQueryKey = ReturnType<typeof getItemQueryKey>;

export function getItemQueryOptions(
  params: GetItemQueryParams,
  config: Partial<RequestConfig> & { client?: typeof fetch } = {}
) {
  const queryKey = getItemQueryKey(params);
  return queryOptions<
    GetItemQueryResponse,
    ResponseErrorConfig<GetItem400 | GetItem422>,
    GetItemQueryResponse,
    typeof queryKey
  >({
    enabled: !!params,
    queryKey,
    queryFn: async ({ signal }) => {
      config.signal = signal;
      return getItem(params, config);
    },
  });
}

/**
 * @summary Retrieve a single item.
 * {@link /store/items}
 */
export function useGetItem<
  TData = GetItemQueryResponse,
  TQueryData = GetItemQueryResponse,
  TQueryKey extends QueryKey = GetItemQueryKey,
>(
  params: GetItemQueryParams,
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetItemQueryResponse,
        ResponseErrorConfig<GetItem400 | GetItem422>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: typeof fetch };
  } = {}
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...queryOptions } = queryConfig;
  const queryKey = queryOptions?.queryKey ?? getItemQueryKey(params);

  const query = useQuery(
    {
      ...getItemQueryOptions(params, config),
      queryKey,
      ...queryOptions,
    } as unknown as QueryObserverOptions,
    queryClient
  ) as UseQueryResult<TData, ResponseErrorConfig<GetItem400 | GetItem422>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
