/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  CreditControllerListLedgerQueryParams,
  CreditControllerListLedgerQueryResponse,
} from "@/gen-backend/types/credit-controller-list-ledger";
import type { RequestConfig, ResponseErrorConfig } from "@/lib/kubb-backend-client";
import fetch from "@/lib/kubb-backend-client";

function getCreditControllerListLedgerUrl() {
  const res = { method: "GET", url: `/api/credit/me/ledger` as const };
  return res;
}

/**
 * @summary List credit ledger entries (paginated)
 * {@link /api/credit/me/ledger}
 */
export async function creditControllerListLedger(
  params: CreditControllerListLedgerQueryParams,
  config: Partial<RequestConfig> & { client?: typeof fetch } = {}
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    CreditControllerListLedgerQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "GET",
    url: getCreditControllerListLedgerUrl().url.toString(),
    params,
    ...requestConfig,
  });
  return res.data;
}
