/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  AccountControllerResumeAccountMutationResponse,
  AccountControllerResumeAccountPathParams,
} from "@/gen-backend/types/account-controller-resume-account";
import type { RequestConfig, ResponseErrorConfig } from "@/lib/kubb-backend-client";
import fetch from "@/lib/kubb-backend-client";

function getAccountControllerResumeAccountUrl(
  publicKey: AccountControllerResumeAccountPathParams["publicKey"]
) {
  const res = { method: "POST", url: `/api/account/resume/${publicKey}` as const };
  return res;
}

/**
 * @summary Reset failure counters on a managed account
 * {@link /api/account/resume/:publicKey}
 */
export async function accountControllerResumeAccount(
  publicKey: AccountControllerResumeAccountPathParams["publicKey"],
  config: Partial<RequestConfig> & { client?: typeof fetch } = {}
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    AccountControllerResumeAccountMutationResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "POST",
    url: getAccountControllerResumeAccountUrl(publicKey).url.toString(),
    ...requestConfig,
  });
  return res.data;
}
