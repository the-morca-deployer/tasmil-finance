{
  "type": "object",
  "properties": {
    "id": { "type": "string", "example": "starter", "description": "Stable human-readable id" },
    "usd": { "type": "number", "example": 5, "description": "USD price (number, not Decimal)" },
    "credits": {
      "type": "number",
      "example": 100,
      "description": "Credits granted on fulfillment"
    },
    "points": { "type": "number", "example": 1000, "description": "Points granted on fulfillment" },
    "bonusPercent": {
      "type": "number",
      "example": 0,
      "description": "Bonus credit percentage advertised on the card (0–100)"
    },
    "sortOrder": {
      "type": "number",
      "example": 10,
      "description": "Display order ascending; lower first"
    }
  },
  "required": ["id", "usd", "credits", "points", "bonusPercent", "sortOrder"],
  "x-readme-ref-name": "CreditPackageDto"
}
