{
  "type": "object",
  "properties": {
    "signedXdr": { "type": "string", "description": "Signed XDR string from user wallet" },
    "publicKey": { "type": "string", "description": "User Stellar public key" },
    "meta": {
      "type": "object",
      "properties": {
        "action": { "type": "string", "enum": ["DEPOSIT", "WITHDRAW", "REBALANCE", "HARVEST"] },
        "protocol": {
          "type": "string",
          "enum": ["TASMIL_VAULT", "BLEND", "SOROSWAP", "AQUARIUS", "PHOENIX", "DEFINDEX"]
        },
        "asset": { "type": "string" },
        "poolLabel": { "type": "string" }
      },
      "required": ["action", "protocol"],
      "x-readme-ref-name": "SubmitAiTxMetaDto"
    }
  },
  "required": ["signedXdr", "publicKey"],
  "x-readme-ref-name": "SubmitAiTxDto"
}
