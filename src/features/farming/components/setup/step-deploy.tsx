"use client";

// Task 13 stub — replaced by full implementation in Task 13.
interface Props {
  publicKey: string;
  asset: string;
  mode: string;
  preset: string;
  estimatedApy: number;
  customMarkets: string[];
  onComplete: () => void;
}

export function StepDeploy(_: Props) {
  return null;
}
